Con el comando

        - docker ps

Se muestran los contenedores que estén activos pero no muestra todos los contenedores. Para poder mostrar todos los contenedores se ejecuta el comando:

        - docker ps -a