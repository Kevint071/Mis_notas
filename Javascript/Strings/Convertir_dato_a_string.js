// Para convertir un tipo de dato a string se puede hacer lo siguiente:

var numero = 1;
var num_string = String(numero);

console.log(typeof(num_string)); // string
console.log(typeof(numero)); // number