Si nos dice error por compilacion de C por algun motivo ejecutamos los siguientes codigos:

        sudo apt-get install gcc

Luego de esto lo escribimos lo siguiente en la terminal:

        sudo apt-get install build-essential

Cuando terminemos esto probamos de nuevo con ./configure


Estos codigos en lo personal los usé para instalar una version de python mas reciente

